# Visual Product Similarity (Amazon Style)

**Tech Stack**: ResNet50 + FAISS + Streamlit

## Quick Start
```bash
pip install -r requirements.txt
python extract_embeddings.py
streamlit run app.py
```

Upload image → Get visually similar products!